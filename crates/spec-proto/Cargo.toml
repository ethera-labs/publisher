# Vendored copy retained until native spec crate integration is complete.
[package]
name = "compose-spec-proto"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
compose-spec.workspace = true
prost.workspace = true

[lints]
workspace = true
