# Vendored copy retained until native spec crate integration is complete.
[package]
name = "compose-spec"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true
